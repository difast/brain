-r requirements.txt

pytest==8.3.4
pytest-asyncio==0.25.2
pytest-cov==6.0.0
aiosqlite==0.20.0
# Test-only: parses the scrape output with the reference implementation, so a
# hand-rolled renderer can't drift out of the exposition format.
prometheus-client==0.21.1
httpx==0.28.1
# Keep this current: UP042 (a str+Enum mixin where StrEnum belongs) only
# started failing the build once ruff was new enough to enforce it by default.
ruff==0.15.8
mypy==1.14.1
